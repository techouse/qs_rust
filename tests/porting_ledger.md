# Test Porting Ledger

## Divergence Decisions

| Behavior | Classification | Rust destination | Reason |
| --- | --- | --- | --- |
| Parameter counting still includes charset sentinel parameters and empty-key pairs | Node-compatible default | `tests/divergences.rs`, `tests/parity_decode.rs` | Sibling ports diverge here, but Rust keeps Node counting semantics as the public default. |
| Top-level dotted keys stay raw at `depth = 0` | Node-compatible default | `tests/divergences.rs` | C# and Kotlin carry special-case behavior here; Rust keeps the upstream Node result. |
| Function filters, custom decoders, custom sorters, custom key/value token encoders, comma-null compaction, and encode depth guards | Shared-port default | `src/options.rs`, `src/decode.rs`, `src/encode.rs`, `tests/divergences.rs` | These are sibling-port extensions Rust now exposes directly in a Rust-idiomatic callback surface. |
| Core temporal leaves plus native runtime conversions | Shared-port default | `src/temporal.rs`, `src/chrono_support.rs`, `src/time_support.rs`, `src/serde.rs`, `tests/feature_matrix.rs` | Rust now exposes `Value::Temporal`, a core `TemporalSerializer`, native `chrono` / `time` conversion helpers, and opt-in serde field helpers. Implicit host-object date detection outside the dynamic value model remains out of scope. |
| Host-object reflection/prototype behavior, cycles, negative/infinite numeric limits, and runtime-specific bridge helpers | Unsupported in Rust | documented only | These depend on JS/Foundation/JVM/.NET runtime features or option ranges Rust does not expose. |

## Ported Test Families

| Source file or family | Rust destination | Status | Node-backed | Reason |
| --- | --- | --- | --- | --- |
| `node-qs/test/parse.js` basic, delimiter, dot, depth, array, charset, parameter-limit, comma cases | `tests/support/cases/node_parse.rs`, `tests/parity_decode.rs` | ported | yes | Core decode semantics map directly to the Rust surface. |
| `node-qs/test/parse.js` prototype pollution, null-prototype, and circular host objects | not imported | skipped | no | JS runtime object-model behavior does not map to the Rust `Value` tree. The Rust custom decoder surface is covered locally instead of mirroring the Node callback tests verbatim. |
| `node-qs/test/stringify.js` basic object/list/null/format/charset/filter-array/sort/dot cases | `tests/support/cases/node_stringify.rs`, `tests/parity_encode.rs` | ported | yes | Core encode semantics map directly to the Rust surface. |
| `node-qs/test/stringify.js` JS date/buffer/cycle/prototype cases and callback-specific host-object assertions | not imported | skipped | no | Rust now exposes filter/sorter/value-encoder hooks, but the JS host-object and cycle cases still do not map to the Rust public contract. |
| `node-qs/test/empty-keys-cases.js` parse empty-key skipping and stringify empty-key encodings | `tests/support/cases/node_parse.rs`, `tests/support/cases/node_stringify.rs` | ported | yes | Empty-key behavior is part of the public API surface. |
| `node-qs/test/utils.js` merge/combine internals that map cleanly to Rust overflow and compact logic | `src/decode.rs`, `src/merge.rs`, `src/compact.rs` unit tests | ported | no | Internal algorithm hardening is relevant even without direct JS host-shape parity. |
| `node-qs/test/utils.js` getter/setter, prototype, function, symbol, and side-effect cases | not imported | skipped | no | JS-specific runtime artifacts do not map to Rust values. |
| `qs.py/tests/unit/decode_test.py` encoded-dot, strict-depth remainder, and mixed encoded-bracket cases | `tests/support/cases/python_decode.rs`, `tests/parity_decode.rs` | ported | yes | These cases fill decode gaps and are verified against Node at runtime. |
| `qs.py/tests/unit/encode_test.py` empty-key encode families | `tests/support/cases/python_encode.rs`, `tests/parity_encode.rs` | ported | yes | Public stringify behavior matches the Rust surface and Node parity harness. |
| `qs.py/tests/unit/thread_safety_test.py` immutable public-options concurrency | `tests/regressions.rs` | ported | no | Rust-only regression for sharing `DecodeOptions`/`EncodeOptions` across threads. |
| `qs.py/tests/unit/utils_test.py` and fast-path parity helpers | `src/decode.rs`, `src/encode.rs` unit tests | ported | no | Relevant to internal non-recursive behavior and path splitting. |
| `qs.py` weakref/signature-adapter/datetime/Decimal/UserDict/UserList/regex host-type cases | not imported | skipped | no | Python object-model and type-coercion artifacts are outside the Rust API. |
| `qs.dart/test/unit/decode_test.dart` public decode cases that agree with Node | `tests/support/cases/dart_decode.rs`, `tests/parity_decode.rs` | ported | yes | The portable subset adds Dart-derived parity for encoded-dot splitting, regex delimiters, depth remainder wrapping, comma overflow, and array-limit fallback cases after verifying Node agrees. |
| `qs.dart/test/unit/decode_test.dart` custom `Pattern` delimiters, negative or infinite numeric limits, map/byte/date/regex inputs, and Dart-specific error-shape assertions | not imported | skipped | no | Rust now has a custom decoder surface, but these remaining cases still depend on unsupported delimiter forms, impossible option ranges, non-query host inputs, or Dart runtime/error contracts. |
| `qs.dart/test/unit/encode_test.dart` public encode cases that agree with Node | `tests/support/cases/dart_encode.rs`, `tests/parity_encode.rs` | ported | yes | The portable subset extends parity for RFC1738 key-only strict-null output, `encode=false` null handling, dotted encode output, comma round-trip, empty-array emission, and empty-key object/list edge cases. |
| `qs.dart/test/unit/encode_test.dart` `BigInt`, `ByteBuffer`, `DateTime`, `Symbol`, cycles, and arbitrary Dart object handling | not imported | skipped | no | Rust now has filter/sorter/value-encoder hooks, but these remaining cases still depend on Dart host-type coercions or cyclic object handling outside the Rust public contract. |
| `qs.dart/test/unit/utils_test.dart` overflow merge/combine, latin1 invalid-escape handling, compact order, and non-recursive helper behavior | `src/decode.rs`, `src/merge.rs`, `src/compact.rs` unit tests | ported | no | These cases harden Rust internals that correspond to the Dart port’s overflow, decode, and compaction helpers. |
| `qs.dart/test/unit/utils_test.dart` `ByteBuffer`, fake encodings, surrogate-pair/lone-surrogate handling, alias-preserving compact cycles, and miscellaneous Dart helper APIs | not imported | skipped | no | Those cases depend on Dart runtime types or Unicode states Rust `String` cannot represent, or they target helper APIs that are not part of the Rust crate. |
| `qs.dart/test/unit/encode_edge_cases_test.dart` | not imported | skipped | no | The applicable omission/filter semantics are covered locally through `FilterResult::Omit`; the remaining cases are dominated by MapBase indexer traps, Dart object coercion, `Undefined` placeholders, and cycle detection outside the Rust surface. |
| `qs.dart/test/unit/array_test.dart` | not imported | skipped | no | These tests exercise Dart container classes and `Undefined` placeholders rather than query-string semantics. |
| `qs.dart/test/unit/list_format_test.dart` | not imported | skipped | no | Rust exposes `ListFormat` directly and does not have Dart’s generator/toString helper API surface. |
| `qs.dart/test/unit/uri_extension_test.dart` | not imported | skipped | no | URI extension wrappers are Dart-specific convenience APIs; the underlying encode/decode semantics are already covered through the main parity suites. |
| `qs.dart/test/unit/fixed_qs_issues_test.dart` | not imported | skipped | no | The applicable `qs#493` behavior is already covered by the existing Node-backed and cross-port parity cases. |
| `qs.dart/test/unit/example.dart` supported public encode/decode examples | `tests/examples.rs` | ported | no | README-friendly Rust smoke tests adapt the applicable public examples directly instead of leaving them implicit in the parity suites. |
| `qs.dart/test/unit/example.dart` `Undefined` and Dart-only host-type examples | not imported | skipped | no | Rust now covers the supported callback-style examples separately, but the remaining examples still depend on runtime types outside the Rust crate. |
| `qs.dart/test/e2e/e2e_test.dart` | not imported | skipped | no | The same round-trip territory is already covered by the Node smoke corpus, typed parity suites, and property tests. |
| `QsNet/DecodeTests.cs` encoded-dot, top-level dot, comma-limit, nested-comma, and empty-list cases that agree with Node | `tests/support/cases/csharp_decode.rs`, `tests/parity_decode.rs` | ported | yes | These fill public decode gaps after verifying Node agrees on each case. |
| `QsNet/DecodeTests.cs` enumerable/map-input `Qs.Decode(...)` behaviors for duplicates, normalized collisions, empty-key skipping, and object leaves | `tests/regressions.rs` | ported | no | These map to Rust `decode_pairs` rather than raw query-string parity. |
| `QsNet/DecodeTests.cs` top-level dot guardrail cases that preserve raw dotted keys at `depth=0` or recover malformed bracket roots differently from Node | not imported | skipped | no | Node remains the semantic oracle, and these C# expectations intentionally diverge from it. |
| `QsNet/EncodeTests.cs` nested dot encoding, allow-empty-list, strict-null, charset-sentinel, and comma `encodeValuesOnly` cases that agree with Node | `tests/support/cases/csharp_encode.rs`, `tests/parity_encode.rs` | ported | yes | These expose Rust-relevant encode edge cases and are verified against Node. |
| `QsNet/EncodeTests.cs` byte-array, `encode=false`, and deep-chain fallback behaviors on the Rust public surface | `tests/regressions.rs`, `src/encode.rs` unit tests | ported | no | These are Rust-applicable behaviors that Node cannot represent directly. |
| `QsNet/UtilsTests.cs` overflow append/shift and sparse compact behavior | `src/decode.rs`, `src/merge.rs`, `src/compact.rs` unit tests | ported | no | Internal overflow/compact mechanics are part of the semantic core. |
| `QsNet/UtilsTests.cs` throw-based decoder fallback branches | not imported | skipped | no | Rust `DecodeDecoder` is infallible, so the literal throwing-decoder cases do not map directly. Invalid-byte fallback and plus-only decode behavior stay covered through local Rust tests instead. |
| `QsNet/DecodeOptionsTests.cs` and `QsNet/EncodeOptionsTests.cs` builder invariants and decode validation limits | `tests/support/cases/csharp_internal.rs`, `tests/regressions.rs` | ported | no | Rust option-builder invariants replace the original `CopyWith` surface, and validation errors are checked through the public API. |
| `QsNet/HardeningRegressionTests.cs` allow-empty-list, byte-array, deep-chain, and strict-null hardening that maps to the Rust surface | `tests/support/cases/csharp_decode.rs`, `tests/support/cases/csharp_encode.rs`, `tests/regressions.rs`, `src/decode.rs`, `src/encode.rs` unit tests | ported | mixed | The Node-compatible subset is in parity suites; Rust-only surface and internal hardening stays local. |
| `QsNet/HardeningRegressionTests.cs` callbacks, remaining date serializer integration details, non-generic maps, reflection helpers, and side-channel cleanup details | not imported | skipped | no | Core temporal leaves and serializer helpers are now covered locally, but these remaining cases still depend on implicit host-object date handling outside `Value`, reflection-heavy replacements, or .NET internals. |
| `QsNet/KeyPathNodeTests.cs` append-empty identity and path materialization semantics | `src/key_path.rs` unit tests | ported | no | The Rust key-path type has equivalent append/materialize behavior worth hardening locally. |
| `QsNet` parameter-limit cases that ignore charset sentinel or decoded empty keys | not imported | skipped | no | Node `qs` counts those parameters; the C# tests intentionally diverge. |
| `QsNet` callback, serializer, reflection, `DateTime`, `Uri`, enum, and non-generic `IDictionary` host-shape tests | not imported | skipped | no | These depend on C# runtime features or surfaces the Rust crate does not expose. |
| `QsNet/EndToEndTests.cs` and `Fixtures/Data/EndToEndTestCases.cs` fixture round trips | not imported | skipped | no | The same territory is already covered by the Node smoke corpus, Rust roundtrip properties, and existing public encode/decode regressions. |
| `QsNet/ExampleTests.cs` | not imported | skipped | no | These are redundant with the existing parity suites, smoke corpus, and crate docs/examples. |
| `QsNet/ExtensionTests.cs` | not imported | skipped | no | C# extension methods do not map to the Rust API surface. |
| `QsNet/FormatExtensionsTests.cs`, `ListFormatExtensionsTests.cs`, and `SentinelExtensionsTests.cs` | not imported | skipped | no | Rust exposes enums directly rather than extension-helper APIs; the underlying semantics are covered elsewhere. |
| `QsNet/UndefinedTests.cs` and `SideChannelFrameTests.cs` | not imported | skipped | no | Rust has no public `Undefined` singleton or side-channel frame type. |
| `QsSwift/DecodeFastPathTests.swift` root-collision, parameter-limit, delimiter, list-limit, and comma cases that agree with Node | `tests/support/cases/swift_decode_fast_path.rs`, `tests/parity_decode.rs` | ported | yes | These are public decode semantics and are verified through the Node-backed harness. |
| `QsSwift/DecodeFastPathTests.swift` structured-scan helpers and flat-finalize preservation | `src/decode.rs`, `src/structured_scan.rs` unit tests | ported | no | These harden Rust internals without depending on Swift-only fast-path entrypoints. |
| `QsSwift/DecodeFastPathTests.swift` legacy-decoder branches and dedicated fast-decoder bailout tests | not imported | skipped | no | Rust does not expose a separate legacy decoder or Swift-specific fast-path API surface. |
| `QsSwift/DecodeTests.swift` public query-string decode cases that agree with Node | `tests/support/cases/swift_decode.rs`, `tests/parity_decode.rs` | ported | yes | The portable subset extends Swift-derived parity for dots, empty ampersands, bracket-prefixed plain keys, top-level numeric brackets, `%uXXXX` ISO-8859-1 literals, empty arrays, depth handling, list/object promotion, jquery-style indices, malformed roots, delimiters, and parameter-limit truncation. |
| `QsSwift/DecodeTests.swift` charset sentinel ordering/default-selection and decode-key protection edge cases | `src/decode.rs` unit tests, `tests/regressions.rs` | ported | no | Rust already hardens the Swift-derived sentinel ordering, unknown-sentinel, numeric-entity, and encoded-dot key-protection behavior locally, so these cases stay in Rust tests instead of being re-imported wholesale through the Node-backed parity harness. |
| `QsSwift/DecodeTests.swift` custom-decoder plain-token and comma-list coverage | `src/decode/tests/flat.rs` | ported | no | Rust mirrors the applicable `DecodeDecoder` behavior locally instead of routing callback cases through the Node-backed parity harness. |
| `QsSwift/DecodeTests.swift` map/data/date/regex object inputs, circular host objects, async wrappers, and non-query convenience overloads | not imported | skipped | no | Those tests depend on Swift-specific host types or async/convenience APIs that do not exist in the Rust crate. |
| `QsSwift/EncoderInternalsTests.swift` allow-dots and multi-key fallback cases that agree with Node | `tests/support/cases/swift_encoder_internals.rs`, `tests/parity_encode.rs` | ported | yes | These exercise public encode output while still covering fast-path fallback conditions. |
| `QsSwift/EncoderInternalsTests.swift` key-path append/materialization behavior | `src/key_path.rs`, `src/encode.rs` unit tests | ported | no | Rust has equivalent path-materialization mechanics worth hardening locally. |
| `QsSwift/EncoderInternalsTests.swift` ARC identity, weak-retain, and dot-encoded cache identity tests | not imported | skipped | no | Rust `KeyPathNode` has no ARC or cached dot-encoded object identity semantics. |
| `QsSwift/EncodeTests.swift` public encode cases that agree with Node | `tests/support/cases/swift_encode.rs`, `tests/parity_encode.rs` | ported | yes | The portable subset extends Swift-derived parity for empty-array omission/emission, empty keys, RFC1738 formatting, single-item `encodeValuesOnly` list formatting, comma round-trip guardrails, nested encodeValuesOnly paths, charset sentinel handling, and Latin-1 numeric entities. |
| `QsSwift/EncodeTests.swift` invalid UTF-8 `Data` visibility under `encode=false` | `tests/regressions.rs` | ported | no | Rust maps this behavior through `Value::Bytes` instead of Foundation `Data`, so the equivalent regression stays local. |
| `QsSwift/EncodeTests.swift` Foundation dates/URLs/optionals, arbitrary host objects, cycles, `Undefined`, and async convenience wrappers | not imported | skipped | no | The applicable callback/filter omission behavior is covered locally; the remaining cases depend on Swift runtime types or wrappers outside the Rust surface. |
| `QsSwift/EncodeTopLevelNormalizationTests.swift` top-level array promotion to string indices | `tests/support/cases/swift_encode.rs`, `tests/parity_encode.rs` | ported | yes | Top-level array normalization maps directly to the Rust public `Value` tree. |
| `QsSwift/EncodeTopLevelNormalizationTests.swift` `NSDictionary`/`OrderedDictionary` host-shape normalization, mixed `AnyHashable` keys, custom sort closures, and encode-true default auto-sort | not imported | skipped | no | Rust already uses ordered string-keyed maps and does not expose arbitrary host objects or callback sorters. |
| `QsSwift/UtilsTests.swift` percent-encoding, scalar decoding, numeric entity interpretation, overflow/compact, and deep-stack helper coverage | `src/decode.rs`, `src/encode.rs`, `src/merge.rs`, `src/compact.rs` unit tests | ported | no | The Rust internals expose equivalent helper and overflow behavior worth hardening locally without mirroring Swift-only utility entrypoints. |
| `QsSwift/UtilsTests.swift` unsupported-type/`Undefined` helpers, Foundation `Data` wrapper APIs, and Swift collection/set-specific merge helpers | not imported | skipped | no | Those tests target Swift runtime scaffolding rather than the Rust query-string/value model. |
| `QsSwift/ModelCoverageTests.swift` public error-helper coverage | `tests/regressions.rs` | ported | no | Rust has equivalent stable inspection helpers on `DecodeError` and `EncodeError`, but not the same NSError-bridging surface. |
| `QsSwift/ModelCoverageTests.swift` NSError bridging, `_UnsafeSendable`, `WeakWrapper`, and `Undefined` singleton helpers | not imported | skipped | no | Rust now exposes callback filters, but it still intentionally omits Foundation bridging, weak-wrapper identity types, and public singleton-style `Undefined` helpers. |
| `QsSwift/DelimiterTests.swift`, `QsSwift/SentinelTests.swift`, `QsSwift/FormatTests.swift`, and `QsSwift/ListFormatTests.swift` | not imported | skipped | no | These mostly exercise Swift helper/description APIs; the underlying delimiter, sentinel, format, and list-format semantics are already covered by Node-backed parity and local regressions. |
| `QsSwift/ConvenienceTests.swift` | not imported | skipped | no | Swift-only convenience overloads such as `encodeOrNil`, `encodeOrEmpty`, and async wrappers do not map to the Rust API surface. |
| `QsSwift/ExampleTests.swift` and `QsSwift/EndToEndTests.swift` | not imported | skipped | no | The same territory is already covered more directly by the Node smoke corpus, typed parity suites, and property/regression tests. |
| `QsSwift/CoreExtrasTests.swift` | not imported | skipped | no | These tests focus on NSError bridging, sentinel helpers, self-cycle behavior, and callback sorters that are either unsupported or already covered through Rust-specific error and acyclic-value regressions. |
| `qs-kotlin/unit/UtilsSpec.kt` overflow append/shift/max-index tracking and compact normalization | `tests/support/cases/kotlin_utils.rs`, `src/decode.rs`, `src/merge.rs`, `src/compact.rs` unit tests | ported | no | These map cleanly to Rust internal overflow and compaction behavior. |
| `qs-kotlin/unit/UtilsSpec.kt` JVM host-type encode/decode, `ByteBuffer`, `URI`, enum, and escape helper cases | not imported | skipped | no | Those cases are JVM-specific or already covered by existing public-surface tests. |
| `qs-kotlin/unit/internal/DecoderInternalSpec.kt` delimiter, charset-sentinel, comma, and bracket-suffix cases that agree with Node | `tests/support/cases/kotlin_decoder_internal.rs`, `tests/parity_decode.rs`, `src/decode.rs` unit tests | ported | yes | Gap-filling public decode semantics are verified against Node; helper-only coverage stays local. |
| `qs-kotlin/unit/internal/DecoderInternalSpec.kt` negative `listLimit`, non-empty-only parameter counting, and over-limit comma truncation | not imported | skipped | no | Kotlin intentionally diverges from Node or uses option ranges the Rust API does not expose. |
| `qs-kotlin/unit/internal/EncoderInternalSpec.kt` nested null-handling and dot-encoding cases that agree with Node | `tests/support/cases/kotlin_encoder_internal.rs`, `tests/parity_encode.rs`, `src/encode.rs` unit tests | ported | yes | These are public encode semantics and also stress the linear-chain fast-path guards. |
| `qs-kotlin/unit/internal/EncoderInternalSpec.kt` custom encoder combinations that map to the Rust surface | `src/encode/tests/helpers.rs` | ported | no | Rust now hardens the applicable `EncodeTokenEncoder` combinations locally, including key tokens, joined comma `TextValue` tokens, and the `encodeValuesOnly` gate that keeps key callbacks disabled when requested. |
| `qs-kotlin/unit/internal/EncoderInternalSpec.kt` `ByteBuffer`/`ByteArray` host handling, cycle/filter behavior, and remaining temporal serializer integration details | not imported | skipped | no | Core temporal leaves and serializer helpers are covered locally, but these remaining cases still depend on JVM host types, broader callback/filter behavior, or implicit date detection outside the Rust value model. |
| `qs-kotlin/unit/DecodeSpec.kt` public decode edge cases that agree with Node | `tests/support/cases/kotlin_decoder_internal.rs`, `tests/parity_decode.rs` | ported | yes | The portable subset adds Kotlin-derived coverage for dot guardrails, depth wrapping, malformed-yet-balanced keys, and charset-sentinel behavior under the Node-backed parity harness. |
| `qs-kotlin/unit/DecodeSpec.kt` negative limits, callback decoders, map-input coercion, `allowDots+depth=0` dot-preservation, and Kotlin-only exception wording | not imported | skipped | no | Those cases depend on unsupported callback surfaces, impossible Rust option ranges, non-query input forms, or semantics that intentionally diverge from Node. |
| `qs-kotlin/unit/EncodeSpec.kt` public encode edge cases that agree with Node | `tests/support/cases/kotlin_encoder_internal.rs`, `tests/parity_encode.rs` | ported | yes | The portable subset extends parity for dot-encoding combinations, nested empty-list emission, sentinel joining, and list/object formatting edge cases. |
| `qs-kotlin/unit/EncodeSpec.kt` remaining temporal serializer integration details, arbitrary JVM objects, cycles, `Undefined`, and custom filter/encoder branches beyond the Rust `Value` model | not imported | skipped | no | Core temporal leaves and serializer helpers are covered locally; the remaining cases target implicit host-type date handling outside `Value`, broader filter behavior, arbitrary JVM objects, or cyclic/object contracts the Rust crate does not expose. |
| `qs-kotlin/unit/models/DecodeOptionsSpec.kt` baseline defaults and Rust-relevant option invariants | `tests/regressions.rs` | ported | no | Rust exposes builders and getters rather than Kotlin `copy`, but the applicable baseline defaults and dot-option invariants are covered locally. |
| `qs-kotlin/unit/models/DecodeOptionsSpec.kt` negative depth construction and Kotlin-specific callback plumbing details | not imported | skipped | no | Rust now exposes a key-aware decoder surface, but these remaining cases still depend on impossible negative option ranges or Kotlin-specific builder/callback wiring. |
| `qs-kotlin/unit/models/EncodeOptionsSpec.kt` baseline defaults and Rust-relevant option invariants | `tests/regressions.rs` | ported | no | Rust-specific regressions cover the applicable defaults and builder invariants without reproducing Kotlin’s callback-heavy builder API. |
| `qs-kotlin/unit/models/EncodeOptionsSpec.kt` custom encoders/serializers, deprecated indices compatibility, builder DSL callbacks, and unsupported charset construction | not imported | skipped | no | Those cases depend on Kotlin callback APIs, deprecated compatibility shims, or charset construction paths that do not exist in Rust. |
| `qs-kotlin/unit/models/DelimiterSpec.kt` non-empty literal delimiter enforcement | `tests/regressions.rs`, `src/decode.rs` unit tests | ported | no | Empty literal delimiters are rejected through the Rust public API, and delimiter splitting semantics are already exercised in Node-backed parity. |
| `qs-kotlin/unit/models/DelimiterSpec.kt` regex factory/equality helper behavior | not imported | skipped | no | Rust exposes the delimiter enum directly rather than Kotlin factory/equality helpers. |
| `qs-kotlin/unit/SentinelSpec.kt` helper accessors around sentinel constants | not imported | skipped | no | The Kotlin sentinel helper API is not public in Rust; the underlying charset-sentinel semantics are already covered by parity encode/decode cases. |
| `qs-kotlin/unit/internal/KeyPathNodeInternalSpec.kt` append-empty reuse and dot-materialization behavior | `src/key_path.rs`, `src/encode.rs` unit tests | ported | no | Rust hardens the equivalent append/materialize behavior locally without reproducing Kotlin’s cached `asDotEncoded()` identity API. |
| `qs-kotlin/unit/internal/KeyPathNodeInternalSpec.kt` cached `asDotEncoded()` identity semantics | not imported | skipped | no | Rust `KeyPathNode` does not expose a cached dot-encoded node object, only materialized path behavior. |
| `qs-kotlin/unit/QsParserSpec.kt` historical parser/stringifier examples | not imported | skipped | no | The applicable public semantics are already covered more directly by the Node corpus, the Kotlin `DecodeSpec`/`EncodeSpec` imports, and existing roundtrip/property coverage. |
| `qs-kotlin/unit/ExampleSpec.kt` documentation-style examples | not imported | skipped | no | These cases are redundant with the parity suites, smoke corpus, and crate-level docs/examples. |
| `qs-kotlin/unit/CollectionSpec.kt` Kotlin collection/`Undefined` emulation helpers | not imported | skipped | no | These tests exercise Kotlin runtime scaffolding rather than the Rust query-string semantics. |
| `qs-kotlin/unit/ExtensionsSpec.kt` Kotlin extension-method wrappers | not imported | skipped | no | Rust has no extension-method surface corresponding to `toQueryMap` or `toQueryString`. |
| `qs-kotlin/e2e/EndToEndTest.kt` and `fixtures/data/EndToEndTestCases.kt` | not imported | skipped | no | The same roundtrip territory is already covered by the Node smoke corpus, parity suites, and Rust property tests. |
| `qs-kotlin/unit/models/UndefinedSpec.kt` | not imported | skipped | no | Rust has no public `Undefined` value. |
| `qs-kotlin/unit/models/WeakWrapperSpec.kt` | not imported | skipped | no | Rust has no public weak-wrapper identity type or GC-driven equality contract. |
| `qs-kotlin/unit/models/FilterSpec.kt` | not imported | skipped | no | Callback/filter APIs are out of scope for the current Rust surface. |
